export type SelectionType = {
  branch: string | null;
  year: string | null;
  subject: string | null;
};

export type SelectionChoicesType = {
  branches: string[];
  academicYears: string[];
  subjects: string[];
};
